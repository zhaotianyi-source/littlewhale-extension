## 描述变更

请描述此 PR 做了什么、修复了什么或新增了什么。

## 关联 Issue

Fixes #<issue 编号>（如适用）

## 检查清单

- [ ] 代码通过 `npm run check`（JS 语法检查）
- [ ] 代码通过 `npm test`（UI 回归测试）
- [ ] 修改了 manifest 时，`manifest.json` 仍是合法 JSON
- [ ] README（英文或中文）已同步更新（如涉及用户可见变化）

## 测试说明

描述你如何验证这些改动（浏览器环境、复现步骤等）。
